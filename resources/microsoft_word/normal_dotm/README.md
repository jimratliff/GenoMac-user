# README related to the Normal.dotm file being installed

## Overview
This directory includes `Normal.dotm`, a macro-enabled Microsoft Word template that is 
installed by GenoMac-user as part of implementing the user’s Microsoft Word preferences.

## Installation location
It is to be installed at the following path:
`~/Library/Group Containers/UBF8T346G9.Office/User Content.localized/Templates.localized/Normal.dotm`.

However, note that, when the intermediate folders are viewed in Finder:
- `User Content.localized` appears simply as `User Content`
- `Templates.localized` appears simply as `Templates`
