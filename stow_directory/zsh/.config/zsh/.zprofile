# Executes Homebrew’s shellenv command, which sets up Homebrew’s environment variables
# and adds the destination directory for binaries installed by Homebrew to PATH.
eval "$(/opt/homebrew/bin/brew shellenv)"
